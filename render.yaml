services:
  - type: web
    name: paquerxs
    runtime: node
    plan: free
    buildCommand: yarn install && yarn build
    startCommand: yarn start
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: ADMIN_USER
        sync: false
      - key: ADMIN_PASS
        generateValue: true
      - key: TOOLSDB_HOST
        sync: false
      - key: TOOLSDB_USER
        sync: false
      - key: TOOLSDB_PASSWORD
        sync: false
      - key: TOOLSDB_DATABASE
        sync: false
